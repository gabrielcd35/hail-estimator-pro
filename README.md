# Hail Estimator Pro

AI-powered PDR estimate auditor.

## Setup

1. Clone repo
2. Run `npm install`
3. Copy `.env.example` to `.env.local` and add your Anthropic API key
4. Run `npm run dev`

## Deploy to Vercel

Connect this repo in vercel.com and add env var `ANTHROPIC_API_KEY`.